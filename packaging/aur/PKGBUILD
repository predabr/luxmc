# Maintainer: Pedro <suporte@luxmc.com>
pkgname=luxmc-bin
pkgver=0.8.0
pkgrel=1
pkgdesc="Luxmc - Linux-first Minecraft Launcher"
arch=('x86_64')
url="https://github.com/predabr/luxmc"
license=('GPL3')
depends=('webkit2gtk-4.1' 'gtk3' 'cairo' 'pango' 'glib2' 'openssl' 'flite')
provides=('luxmc')
conflicts=('luxmc')
source=("https://github.com/predabr/luxmc/releases/download/v${pkgver}/luxmc_${pkgver}_amd64.deb")
sha256sums=('SKIP')

package() {
    tar -xf data.tar.gz -C "${pkgdir}"
}
